query GetRecentTransactions($isRecent: Boolean) {
  Transactions(isRecent: $isRecent) {
    ok
    transaction {
      id
      product {
        id
        name
      }
      price
      qty
      total
      verBy {
        id
        email
        username
        position
      }
      createdAt
    }
  }
}
