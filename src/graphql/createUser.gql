mutation createUser($input: userInput) {
  createUser(input: $input) {
    ok
    user {
      id
      email
      username
      npwp
      position
      address
      phoneNumber
      accessToken
    }
    error {
      message
    }
  }
}
